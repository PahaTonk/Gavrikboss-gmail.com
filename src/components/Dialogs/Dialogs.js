import React, { Fragment } from 'react';
import styles from './dialogs.module.scss';

const Dialogs = () => {
    return (
        <Fragment>
            
        </Fragment>
    );
};

export default Dialogs;